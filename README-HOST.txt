Upload to the SITE ROOT (same folder as index.html), do not nest.

Replace:
  sitemap.xml
  robots.txt
  _redirects   (Cloudflare Pages / Netlify)

Then in Google Search Console:
  Sitemaps → add https://clarity-dawah.fyi/sitemap.xml → Request indexing
  for /prepare-for-death/ and /islamic-will/ first.

Do NOT replace index.html with this zip — the working NurOS app stays as index.html.
